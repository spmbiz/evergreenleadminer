name: GWS Qwen One-Shot Canary

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-slim
    steps:
      - run: echo 'Retired from automatic hot path; use GWS Qwen Contract Canary and GWS Semantic Live Canary.'
