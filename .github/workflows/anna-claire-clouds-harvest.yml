name: Anna Claire Clouds 300 scene still harvest

on:
  push:
    branches:
      - anna-claire-clouds-harvest
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  harvest:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: tools/anna_claire_clouds_harvest/requirements.txt
      - name: Install dependencies
        run: pip install -r tools/anna_claire_clouds_harvest/requirements.txt
      - name: Harvest 300 public scene thumbnails and poster stills
        working-directory: tools/anna_claire_clouds_harvest
        env:
          TARGET_COUNT: "300"
        run: python scene_thumb_harvest_300.py
      - name: Report count
        working-directory: tools/anna_claire_clouds_harvest
        run: echo "Collected $(cat SCENE300_COUNT.txt 2>/dev/null || echo 0)"
      - name: Upload 300-scene ZIP
        uses: actions/upload-artifact@v4
        with:
          name: anna-claire-clouds-300-scene-stills
          path: |
            tools/anna_claire_clouds_harvest/anna_claire_clouds_300_scene_stills.zip
            tools/anna_claire_clouds_harvest/SCENE300_COUNT.txt
          if-no-files-found: error
          retention-days: 7
