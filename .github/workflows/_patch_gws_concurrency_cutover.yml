name: One-shot GWS concurrency cutover

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/_patch_gws_concurrency_cutover.yml'

permissions:
  contents: write
  actions: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Patch GWS concurrency safely
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          p=Path('.github/workflows/gws-pending-search-verify.yml')
          s=p.read_text(encoding='utf-8')
          old="concurrency:\n  group: gws-pending-search-verify-fleet\n  cancel-in-progress: false\n"
          expr='$' + "{{ github.event_name == 'push' }}"
          new=("concurrency:\n"
               "  group: gws-pending-search-verify-fleet\n"
               "  # A code push supersedes stale architecture runs; scheduled/refill passes stay serialized.\n"
               f"  cancel-in-progress: {expr}\n")
          if s.count(old) != 1:
              raise SystemExit(f'expected exactly one GWS concurrency block, found {s.count(old)}')
          p.write_text(s.replace(old,new),encoding='utf-8')
          PY
          grep -A4 '^concurrency:' .github/workflows/gws-pending-search-verify.yml
      - name: Commit patch and remove one-shot workflow
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/_patch_gws_concurrency_cutover.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .github/workflows/gws-pending-search-verify.yml .github/workflows/_patch_gws_concurrency_cutover.yml
          git commit -m 'gws: let code pushes supersede stale fleet runs'
          git pull --rebase origin main
          git push origin HEAD:main
      - name: Cancel stale GWS runs and launch fresh main pass
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh run list --workflow gws-pending-search-verify.yml --limit 50 --json databaseId,status \
            --jq '.[] | select(.status=="in_progress" or .status=="queued" or .status=="pending" or .status=="waiting" or .status=="requested") | .databaseId' \
            | while read -r id; do
                [ -n "$id" ] || continue
                echo "Cancelling stale GWS run $id"
                gh run cancel "$id" || true
              done
          sleep 3
          gh workflow run gws-pending-search-verify.yml --ref main
          echo 'Fresh GWS main pass dispatched.'
