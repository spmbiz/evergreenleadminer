name: Repair Ralph Breaks the Internet pack

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive:
    runs-on: ubuntu-latest
    steps:
      - run: echo Superseded by the official Disney Video repair workflow.
